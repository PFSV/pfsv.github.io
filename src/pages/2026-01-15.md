44서버

서버 보안점 관련 패치를 하다가, 방화벽을 켰는데 firewalld 외에도 nft라는게 있고, iptables을 통해 주소록을 알아내는거 같음. 내부에 규칙도 있음. kube가 이 주소를 먼저 채갈수도 있고, 세부적인 규칙들도 있는 것 같음..
dns ? 라는걸 끄니까 연결 속도가 빨라짐

논문
Temporal structure of natural language processing in the human brain corresponds to layered hierarchy of large language models -> 임베딩 벤치마크 생김

보고서 생성
* langraph있는 흥국 파이썬 이미지 받음 -> 44서버
* vllm 버젼 이미지 받고 gpt-oss-120b MoE를 offload해서 써보려고 함 
https://www.reddit.com/r/LocalLLaMA/comments/1mn12i2/any_tipsadvice_for_running_gptoss120b_locally/?tl=ko

https://docs.vllm.ai/projects/recipes/en/latest/OpenAI/GPT-OSS.html#installation-from-pre-built-wheels


임커밋

* 똑똑한 전처리
Data eda : Generator
def : 
list vs generator

데이터를 전부 읽는다는 개념만 있고 요청시 읽는거 -> lazy evaluation 
(지금 당장 계산하고 메모리에 저장하지 않고, 필요시)

반대개념 -> eager evaluation

Generator : lazy evaluation을 실제로 구현한 도구, 값을 하나씩 뽑는객체, Next(). 다음 값이 필요하면 그때 계산, 한 번 지나가면 끝

* 몰라도 문제는 없지만

1. 숫자
1e+8 = 1 뒤에 0 8개,
_ = ,

2. 내수용, 외부용 코드

_ 로 시작하는 _func() 은 내수용 코드, import 시 처리 안 됨
내수용으로만 씀

3. @에 대하여
@를 써서 함수를 wrap해 재사용성을 높임
추가 기능을 설치할 때 g(f(x))처럼 쓰는것이되, g함수를 또 쓰려함. 
@를 막고 문서 등 원활하려면 wraps가 있다는데 잘 모르겠다.

* Neural ODE

방정식의 해 = 등식을 참이 되게 해주는 미지수의 값
미분방정식의 해 = 등식이 참이 되게 해주는 미지함수

Neural ODE =  미분방정식의 해를 딥러닝 모델을 활용해 수치적으로 푼다
수치적으로 푼다 = 그 값을 정확히 구할 수 없어 무수히 많은 연산으로 근삿값을 구한다.

미분방정식의 해를 수치적으로 구한다 = 해 함수를 구할 수는 없으나 모든 인풋에 대한 아웃풋의 수치 근사값을 낸다.
... (이후)
